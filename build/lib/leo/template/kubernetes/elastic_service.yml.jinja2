apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-elastic-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: elastic
  ports:
    - name: http
      protocol: TCP
      port: 9200
      targetPort: 9200
