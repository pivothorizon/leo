apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-main-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: main
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000