apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-prometheus-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: prometheus
  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090
