import marshmallow as ma


class {{ project_name }}RequestSchema(ma.Schema):
    """
    Specifies the parameters {{ model_name }} instances need to initialize.
    """
{% if not use_falcon %}
    class Meta:
        strict = True
{% endif %}

    """
    TODO: Specify the parameters, if any:

    string_parameter = ma.fields.String()
    required_json_obj = ma.fields.Dict(required=True)
    optional_int = ma.fields.Integer(required=False, missing=42)  # Defaults to 42 if not present in the request
    """


class {{ project_name }}{{ schema_name }}ResponseSchema(ma.Schema):
    """
    Specifies the values {{ model_name }}.predict will return as extra_data.
    """
{% if not use_falcon %}
    class Meta:
        strict = True
{% endif %}

    predictions = ma.fields.List(ma.fields.Float())

    # TODO: Specify the return values, if any
