apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-logstash-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: logstash
  ports:
    - name: http
      protocol: TCP
      port: 5959
      targetPort: 5959
