apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-kibana-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: kibana
  ports:
    - name: http
      protocol: TCP
      port: 5601
      targetPort: 5601