apiVersion: v1
kind: Service
metadata:
  name: {{ project_name.lower() }}-db-service
spec:
  selector:
    app: {{ project_name.lower() }}
    type: db
  ports:
    - name: http
      protocol: TCP
      port: 5432
      targetPort: 5432