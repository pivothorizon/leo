apiVersion: v1
kind: Service
metadata:
  name: testleo-main-service
spec:
  selector:
    app: testleo
    type: main
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000