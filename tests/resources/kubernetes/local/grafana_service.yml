apiVersion: v1
kind: Service
metadata:
  name: testleo-grafana-service
spec:
  selector:
    app: testleo
    type: grafana
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000