apiVersion: v1
kind: Service
metadata:
  name: testleo-elastic-service
spec:
  selector:
    app: testleo
    type: elastic
  ports:
    - name: http
      protocol: TCP
      port: 9200
      targetPort: 9200
