apiVersion: v1
kind: Service
metadata:
  name: testleo-prometheus-service
spec:
  selector:
    app: testleo
    type: prometheus
  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090
