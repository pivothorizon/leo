apiVersion: v1
kind: Service
metadata:
  name: testleo-kong-service
spec:
  selector:
    app: testleo
    type: kong
  ports:
    - name: proxy
      protocol: TCP
      port: 8000
      targetPort: 8000
    - name: admin
      protocol: TCP
      port: 8001
      targetPort: 8001